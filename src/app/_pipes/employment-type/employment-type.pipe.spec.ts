import { EmploymentTypePipe } from './employment-type.pipe';

describe('EmploymentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new EmploymentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
