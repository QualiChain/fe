import { LevelTypePipe } from './level-type.pipe';

describe('LevelTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LevelTypePipe();
    expect(pipe).toBeTruthy();
  });
});
